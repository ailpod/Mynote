---
date: 2025-05-28
---
---
# http协议

> HTTP（HyperText Transfer Protocol，超文本传输协议）是互联网上应用最为广泛的一种网络协议。HTTP基于客户端-服务端模型，是一种无状态的协议，意味着每个请求都是独立的，服务器不会保留之前的请求信息。

---
### 简介 

 http 通信双方是客户端(浏览器) 和服务器
  
	 - 客户端通过URL地址基于HTTP协议访问服务器

	 - 服务器基于HTTP协议将响应的内容返回给客户端

### 特性

1.基于 **TCP/ IP** 协议：http是基于此协议之上的应用层协议

2.基于 **请求-响应模式** ：即只有客户端发消息，服务端才能响应

3.无状态保存：例如此次登陆网站，下次再登陆需要重新登陆，因为不对访问做记录

4.短连接：浏览器和服务器每做一次http操作，就建立一次连接，任务结束立即中断


### 请求与响应协议

![[Pasted image 20250528015411.png]]

>[!example]+ 请求协议
>
>请求首行、请求头、请求体
>
>一个完整的**URL**包括：协议、IP、端口、路径、参数
>
>![[Pasted image 20250528021204.png]]

- 请求首行包括请求方式(post & get)、请求路径和请求参数( get方式只能将参数存在这里)，参数和路径用？分隔

- content-type、user-agent是请求头，前者用于给服务器发注释信息 ==注明是什么格式的字符串(如 json url 等)==，由许多键值对组成，后者表示使用什么操作系统/浏览器发出

- 请求头隔空格下面的是请求体，只有请求方式是post的时候才会有数据

>[!example]+ 响应协议
>
>响应首行、响应头、响应体
>
>![[Pasted image 20250528203529.png]]
>

-  响应首行包括三部分：遵循的协议、状态码和状态码的解释


---
## restful接口规范

>应用程序编程接口（Application Programming Interface，API接口），就是应用程序对外提供了一个操作数据的入口，这个入口可以是一个函数或类方法，也可以是一个url地址或者一个网络地址。当客户端调用这个入口，应用程序则会执行对应代码操作，给客户端完成相对应的功能。
>
>当然，api接口在工作中是比较常见的开发内容，有时候，我们会调用其他人编写的api接口，有时候，我们也需要提供api接口给其他人操作。由此就会带来一个问题，api接口往往都是一个函数、类方法、或者url或其他网络地址，不断是哪一种，当api接口编写过程中，我们都要考虑一个问题就是这个接口应该怎么编写？接口怎么写的更加容易维护和清晰，这就需要大家在调用或者编写api接口的时候要有一个明确的编写规范！！！
>
>为了在团队内部形成共识、防止个人习惯差异引起的混乱，我们都需要找到一种大家都觉得很好的接口实现规范，而且这种规范能够让后端写的接口，用途一目了然，减少客户端和服务端双方之间的合作成本。
>
>目前市面上大部分公司开发人员使用的接口实现规范主要有：restful、RPC。
>
>REST全称是Representational State Transfer，中文意思是表述（编者注：通常译为表征）性状态转移。它首次出现在2000年Roy Fielding的博士论文中。
>
>RESTful是一种专门为Web 开发而定义API接口的设计风格，尤其适用于前后端分离的应用模式中。
>
>关键：面向资源开发
>
>这种风格的理念认为后端开发任务就是提供数据的，对外提供的是数据资源的访问接口，所以在定义接口时，客户端访问的URL路径就表示这种要操作的数据资源。
>
>而对于数据资源分别使用POST、DELETE、GET、UPDATE等请求动作来表达对数据的增删查改。

| 请求方式   |     | 请求地址       |     | 后端操作       |
| ------ | --- | ---------- | --- | ---------- |
| POST   |     | /student/  |     | 增加学生       |
| GET    |     | /student/  |     | 获取全部学生     |
| GET    |     | /student/1 |     | 获取 id为1的学生 |
| PUT    |     | /student/1 |     | 修改 id为1的学生 |
| DELETE |     | /student/1 |     | 删除 id为1的学生 |

---

# quick start

>[!example]
1.导入fastapi模块
2.创建一个app实例
3.编写一个路径操作装饰器
4.编写一个路径操作函数
5.定义返回值
6.运行开发服务器

```
from fastapi import FastAPI                                                   ----1
import uvicorn                   

app =  FastAPI()                                                                    ----2 
  
@app.get("/")                                                                         ----3

async def root():                                                                     ----4
    return {"message": "Hello World"}  
    
@app.get("/shop")  

async def shop():  
    return {"shop":"404 not found" }                                         ----5
  
if  __name__ == "__main__":                                                ----6
    uvicorn.run("t1:app",host="127.0.0.1",port=8000,reload=True)
```

---
